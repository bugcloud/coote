!!!
%html
  %head
    %title Coote
    = stylesheet_link_tag %w(scaffold default all)
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    = render 'layouts/header'
    %section#content.body
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"
      = yield 
    =render 'layouts/footer'
